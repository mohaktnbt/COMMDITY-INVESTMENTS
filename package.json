{
  "name": "commodity-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time commodity investments monitor — India-first, Bloomberg-grade intelligence for commodity traders",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
